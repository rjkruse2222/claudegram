# Telegram Configuration
TELEGRAM_BOT_TOKEN=<from @BotFather>
ALLOWED_USER_IDS=<comma-separated telegram user ids>

# Claude Configuration (optional - leave empty to use Claude Max subscription)
# ANTHROPIC_API_KEY=<your anthropic api key>
# CLAUDE_PATH=/path/to/claude  # Optional: path to claude executable if not in PATH

# Workspace (defaults to $HOME/Workspace if not set)
# WORKSPACE_DIR=/path/to/your/workspace

# Bot Behavior
STREAMING_MODE=streaming
STREAMING_DEBOUNCE_MS=500
MAX_MESSAGE_LENGTH=4000
